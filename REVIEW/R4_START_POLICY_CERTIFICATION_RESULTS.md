# R4 Start-Policy Certification Results

**Session:** R4 — Reboot Candidate Construction and Survival Attack  
**Frozen architecture:** Bidirectional Certified Frontier Reboot  
**Result of certification test:** **FAILED**

## 1. Test object

R4 used the prospectively frozen full-rule state model and two-phase White program from `R4_REBOOT_SURVIVAL_HYPOTHESIS.md`.

White program:

- QUIET: `ANSWER_CHECK OR CENTER_PAWN`;
- irreversible switch when `FORCING_MOVE_AVAILABLE`;
- TACTICAL: `ANSWER_CHECK OR CAPTURE OR GIVES_CHECK`.

Black moves are never policy-pruned.

Discovery branch D begins with e2-e3/e2-e4. The held-out branch is documented separately in `R4_HELDOUT_RESULTS.md`.

## 2. Exact START_REACHABLE ply-6 frontier

The audit-local generator reproduced standard-start perft 20 / 400 / 8,902 / 197,281 / 4,865,609 through depths 1–5 and exactly reproduced the frozen R3 D/H policy counts before the R4 run.

Under the frozen R4 two-phase program, D reaches:

- **158,947** unique exact full theorem states at ply 6;
- **118,093** distinct board placements;
- HISTORY_MULTIPLIER = **1.34595x**;
- maximum exact theorem states for one board placement = **15**;
- **185,219** legal Black edges represented during the expanded prefix;
- **13,225 / 107,661 = 12.2839%** retained White edges during the prefix;
- policy-phase collisions on one exact theorem state = **0**.

Every state is generated by an explicit legal path from the standard initial state.

## 3. Frozen cohort

The prospectively defined D cohort contains **1,024** exact full states:

- 512 with `FORCING_MOVE_AVAILABLE = true`;
- 512 with `FORCING_MOVE_AVAILABLE = false`;
- selected lexicographically by canonical full-state identity inside each stratum.

No outcome label or engine score participates in cohort selection.

Cohort material count ranges from **30 to 32 pieces**. There are:

- **0** direct terminal/draw targets;
- **16** states where White is initially in check.

Because every cohort state has at least 30 pieces and the frozen remaining depth is at most five plies, no line can enter a 3–4 piece G12 KQK/KRK/KPK/DEAD object. Thus no missing G12 table lookup can affect the R4 result.

## 4. Exact policy certification

The frozen depth-selection rule tests 1, 3 and 5 remaining plies on D.

| Remaining plies | Certified | Tested | Coverage | Memo configurations | Explored edges | Black edges examined | Solver wall |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0 | 1,024 | **0.0000%** | 2,767 | 1,768 | 0 | 0.0533 s |
| 3 | 0 | 1,024 | **0.0000%** | 7,814 | 7,340 | 1,743 | 0.1742 s |
| 5 | 0 | 1,024 | **0.0000%** | 17,872 | 18,013 | 5,093 | 0.4263 s |

No depth reaches the frozen 25% discovery threshold. The rule therefore freezes depth 5 and records failure.

`CERTIFIED_POLICY_COVERAGE = 0 / 1,024 = 0.0000%`.

`CERTIFICATION_RESIDUAL = 1,024 / 1,024 = 100.0000%`.

## 5. Black-reply closure

The START_REACHABLE structural prefix represents every generated Black legal reply by construction; no opponent pruning is used.

There are **no certified Black proof nodes**, because there are no certified cohort roots. Therefore proof-level 100% Black closure is vacuous and is **not counted as a positive gate**.

The certification recursion only calls a state certified at a Black node after checking every legal reply. No such positive Black node was required by an accepted proof because the accepted proof set is empty.

## 6. Truth leakage

Complete local W/D/L-labelled states used before target/policy freeze:

**0**.

`LOCAL_TRUTH_LEAKAGE = 0%`.

No engine evaluation, opening book, named line, square exception, state-ID patch or board-only merge was used.

## 7. Interpretation

R4 does not merely miss the 25% threshold. The frozen self-certifying target mechanism certifies **no** D state at all.

The result therefore fails to convert R3 strategy compression into proof compression at this scope.

The correct inference is not that the tested states are losing. They may be winning, drawing or losing in full chess. The exact R4 inference is narrower:

> none of the 1,024 prospectively selected D policy states can be certified, within the frozen five-ply local proof budget and frozen target grammar, by forcing terminal White win, exact draw, compatible G12 dependency entry, or recursive composition of those objects.
