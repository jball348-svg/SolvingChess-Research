# Kick off reboot pilot Part 1

Paste the following into a new session with GitHub access.

---

@GitHub https://github.com/jball348-svg/SolvingChess-Research

Run **Part 1 (RP1): benchmark and baseline** of the new proof discovery reboot
pilot. Do the work in the repository and commit/publish the resulting files.
This request authorizes Part 1 only.

The question we are preparing to test is:

> Does an accumulating library of exact, reusable chess proofs reduce the fresh
> discovery work needed for harder, strategically useful truth, after charging
> acquisition, dependencies, verification and application?

Read the active root AGENTS.md, STATUS.md and PILOT_PLAN.md. Use archive/README.md
to retrieve only the historical references needed. The old G/R instructions and
state are archived evidence; do not restart their conveyor or repair their gates.

Execute the complete Part 1 specification in PILOT_PLAN.md:

1. Reproduce an available exact reference result, preferably from the G12 bundle,
   in temporary storage or pilot/ without modifying the archive. Record actual
   source, commands, results, hashes and semantic limitations. Late-review source
   hashes alone do not establish that a runnable implementation exists.
2. Choose a small main chess family with at least four meaningful growth rungs
   and two held-out stress variants. Test ordinary counterplay as well as a
   rules/history issue. Explain what grows and why lower proofs might help. Keep
   decisive held-out outcomes out of selection. Historical published results are
   regression controls, not unseen tests.
3. Implement/adapt a competent exact baseline, a certificate-checking seam and
   cost instrumentation. Run known controls and the smallest development smoke
   case to establish feasibility. Use the Part 1 calibration budget; make routine
   implementation choices without asking me to select the arena or machinery.
4. Freeze one concise pilot/PROTOCOL.md. Specify game semantics, queries, candidate
   discovery procedure, proof objects, information access, library update schedule,
   comparators, coverage/cost metrics, budgets, repeat policy and justified decision
   margins. Compare ordinary caching, fixed proofs and accumulating proofs fairly.
   Charge all acquisition and failed-search costs; allow for later amortization.
   Heuristics may propose candidates, but exact verification must establish claims.
5. Add only the runnable source, necessary checks and compact evidence needed for
   that experiment. Begin pilot/RESULTS.md, update STATUS.md, and commit the
   protocol freeze before any decisive Part 2 evaluation.

Do not run the discovery, compounding or transfer campaigns yet. Do not create a
new distributed system, proof store, long roadmap or stack of governance files.
If the experiment cannot be made sound and feasible within this bounded part,
record the precise blocker rather than inventing a success or expanding the pilot.

Finish with the reference result replayed, the selected benchmark and comparators,
the exact commands, the frozen protocol commit, material limitations, and either
READY_FOR_RP2 or BLOCKED. Stop at the end of Part 1.
